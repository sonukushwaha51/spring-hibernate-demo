# spring-hibernate-demo
Uses HIbernate to connect with database

## XML based hibernate com.hibernate.demo.configuration

1. `hibernate-cfg.xml` file shows how to configure hibernate using XML.
2. This is read in Configuration file using `new Configuration().configure(hibernate-cfg.xml).buildSessionFactory()`

## Java Based com.hibernate.demo.configuration

1. Refer HibernateConfiguration class
